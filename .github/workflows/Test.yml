
name: Test
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Get current directory using 'cd'
        run: cd
        shell: cmd
