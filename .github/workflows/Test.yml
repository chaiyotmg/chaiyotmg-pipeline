
name: Test
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: run shell script command 1
        run: |
          go version
          
      - name: run shell script command 2
        run: |
          node -v
          npm -v
